{
    "/.*\\.fandom\\.com": {
        "description": "Fandom Wiki",
        "mode": "lambda",
        "lambda": "const name=url.host.split('.')[0];const u=new URL(`https://antifandom.com/${name}${url.pathname}`);return u;",
        "author": "PRO-2684"
    }
}